# --- Variables de Entorno para Markdown2Video ---
# Copia este archivo a .env y rellena los valores para tu entorno local.
# En producción (AWS), estas variables deben ser configuradas en el panel de Elastic Beanstalk.

# --- Base de Datos ---
DB_HOST=localhost
DB_NAME=nombre_de_tu_bd
DB_USER=tu_usuario_bd
DB_PASS=tu_contraseña_bd

# --- Configuración de la Aplicación ---
# Define el entorno. Usa 'development' para desarrollo local y 'production' en el servidor.
APP_ENV=development

# --- Servicios Internos ---
# URL donde se está ejecutando el servicio de Node.js para las conversiones.
# Por defecto, se asume que corre en el mismo servidor en el puerto 3000.
NODE_SERVICE_URL=http://localhost:3000
